<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper

 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.UserMapper">

	<select id="selectUserLogin" parameterType="string" resultType="org.zerock.domain.User">
		SELECT 
			user_id as userId
			,user_pwd as userPwd
		FROM user WHERE user_id = #{userId}
	</select>
	
	<select id="selectAll" resultType="org.zerock.domain.User">
		select
			user_id as userId,
			user_pwd as userPwd,
			user_name as userName,
			user_email as userEmail,
			user_cp as userCp,
			user_addr as userAddr,
			user_daddr as userDaddr,
			regDate,
			joinDate,
			user_grade as userGrade
		from user
	</select>
	
	
	<delete id="delete">
		delete from user where user_id=#{userId}
	</delete>
	
	
</mapper>